# Phase 4 - Task 11b: `/contact` お問い合わせページ実装

**目的:** 幅広い問い合わせの窓口となる `/contact` ページを実装する。Task 11a の応募フォームとは別物(住所なし・問い合わせ項目選択あり)。送信後は同ページ内で完了メッセージを表示する。

---

## コンテキスト

### 前提環境
- **Phase 4 Task 11a 完了済み**: `/apply` 削除、応募フォームを `/jobs/[slug]` 内に統合済み
- 現状 `/contact` にアクセスすると404になる(Task 09-10 では対象外だったため)

### 必ず参照すべきドキュメント
- **`docs/05-sitemap.md` §3.3(`/contact` お問い合わせ)** ← フォーム項目の確定仕様
- **`docs/06-messaging.md` §4(ヒーローコピー)・§10(マイクロコピー)** ← 文言の確定仕様
- **`docs/04-design-tokens.md`** ← カラー・スペーシング
- **`components/job/ApplyForm.tsx`**(Task 11a 参照、フォームパターンの参考)

---

## やってほしいこと

### 1. `app/contact/page.tsx` の新規作成

**確定フォーム項目(`docs/05-sitemap.md` §3.3 より、一字一句正確に):**

| # | 項目 | 入力タイプ | 必須 |
|---|---|---|---|
| 1 | 問い合わせ項目 | select(4択) | 必須 |
| 2 | 名前 | text | 必須 |
| 3 | フリガナ | text | 必須 |
| 4 | メールアドレス | email | 必須 |
| 5 | 電話番号 | tel | 任意 |
| 6 | 問い合わせ内容 | textarea | 必須 |
| 7 | プライバシーポリシー同意 | checkbox | 必須 |

**問い合わせ項目の選択肢(確定):**
- 求人について
- イベントについて
- 観光について
- その他

**住所欄: なし**(問い合わせフォームには住所不要)
**郵便番号自動補完: なし**(Task 11a の応募フォームのみ)

**ヒーローコピー(`docs/06-messaging.md` §4 より):**
- メイン: 「お気軽にどうぞ。」
- サブ: 「求人・暮らし・取材まで、なんでも。」

**送信ボタン:** 「送信する」

**送信後:**
- 同ページ内でフォームを非表示にし、「お問い合わせを受け付けました。」(`docs/06-messaging.md` §10.3)を表示
- 別ページへの遷移はしない
- 「求人を見る → /jobs」リンクを完了メッセージ下に表示(任意)

**バリデーション(Task 11a と同様のパターン):**
- 必須項目が空: 「入力してください」
- メール形式不正: 「メールアドレスを確認してください。」
- フリガナにひらがな・漢字が入力された場合: 「カタカナで入力してください。」
- 送信中: 「送信中...」
- 送信ボタン disabled 時: 「必須項目を入力してください」

**送信処理:** 実装しない(TODO コメントを残す)

**参照モード**:

```tsx
"use client";
import { useState } from "react";

const INQUIRY_OPTIONS = [
  "求人について",
  "イベントについて",
  "観光について",
  "その他",
];

export default function ContactPage() {
  const [isSubmitted, setIsSubmitted] = useState(false);
  const [isSubmitting, setIsSubmitting] = useState(false);

  async function handleSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault();
    setIsSubmitting(true);
    // TODO: 実際の送信処理を後で実装
    await new Promise((r) => setTimeout(r, 1000));
    setIsSubmitted(true);
    setIsSubmitting(false);
  }

  if (isSubmitted) {
    return (
      <main>
        <p>お問い合わせを受け付けました。</p>
        {/* TODO: 必要であれば「求人を見る」リンク */}
      </main>
    );
  }

  return (
    <main>
      {/* ヒーロー: お気軽にどうぞ。 / 求人・暮らし・取材まで、なんでも。 */}
      <form onSubmit={handleSubmit}>
        {/* 7項目 */}
      </form>
    </main>
  );
}
```

### 2. 共通フォームコンポーネントの活用

Task 11a で `ApplyForm.tsx` を実装した際に、`components/ui/FormField.tsx` 等の共通フォームコンポーネントが作られている場合はそれを活用する。作られていない場合は Task 11a のパターンに合わせて実装する(重複を避ける)。

---

## 成果物

```
app/
└── contact/
    └── page.tsx      (新規)
```

---

## 制約・前提

- フォーム項目の内容・順序・ラベルは `docs/05-sitemap.md` §3.3 から変更しない
- 問い合わせ項目の4択は確定仕様から変更しない
- 住所欄・郵便番号自動補完は**追加しない**(Task 11a の応募フォームのみ)
- マイクロコピーは `docs/06-messaging.md` §10 から正確に引用する
- 送信処理は実装しない(TODO コメントを残す)
- TypeScript の型エラーを出さない(`any` 禁止)
- Header・Footer・ColumnBoard・3D関連・WordPress側ファイルへの変更は行わない

---

## やってはいけないこと

- ❌ **住所欄・郵便番号自動補完を追加する**(Task 11a の応募フォームのみの機能)
- ❌ **問い合わせ項目の選択肢を変更する**(確定仕様)
- ❌ **`/contact/thanks` 等の別ページへ遷移させる**(同ページ内完結)
- ❌ **フォーム項目の順序・ラベルを変更する**
- ❌ **Header.tsx, Footer.tsx, ColumnBoard.tsx の変更**
- ❌ **WordPress側ファイルへの変更**

---

## Git 運用ルール

コミットメッセージ例:
- `Phase 4 Task 11b: /contact お問い合わせページ実装`

---

## レビュー基準(Claude Code レビュー用チェックリスト)

### ファイル構造
- [ ] `app/contact/page.tsx` が存在するか
- [ ] Header・Footer等が変更されていないか

### フォーム項目の正確性(最重要)
- [ ] 7項目が `docs/05-sitemap.md` §3.3 と一致しているか
- [ ] 問い合わせ項目の4択が正確か(「求人について」「イベントについて」「観光について」「その他」)
- [ ] 電話番号が**任意**になっているか(応募フォームとの違い)
- [ ] 住所欄・郵便番号フィールドが**存在しない**か
- [ ] プライバシーポリシーチェックボックスに `/privacy` へのリンクが含まれているか

### ヒーローコピー
- [ ] メイン: 「お気軽にどうぞ。」
- [ ] サブ: 「求人・暮らし・取材まで、なんでも。」

### 送信・完了フロー
- [ ] 送信後にフォームが非表示になり「お問い合わせを受け付けました。」が表示されるか
- [ ] 別ページへの遷移がないか(同ページ内完結)
- [ ] TODOコメント(送信処理)が残されているか

### マイクロコピー
- [ ] バリデーションエラー: 「入力してください」
- [ ] メールエラー: 「メールアドレスを確認してください。」
- [ ] フリガナエラー: 「カタカナで入力してください。」
- [ ] 送信中: 「送信中...」
- [ ] 送信ボタン disabled 時: 「必須項目を入力してください」

### コード品質
- [ ] Task 11a で作成した共通フォームコンポーネントを活用しているか(重複実装になっていないか)
- [ ] TypeScript の型エラーがないか(`any` 未使用)

---

## 完了後の確認手順

1. `npm run dev` を起動
2. `/contact` にアクセスし、ヒーロー「お気軽にどうぞ。」と7項目フォームが表示されることを確認
3. 問い合わせ項目のセレクトに4択(求人/イベント/観光/その他)が表示されることを確認
4. 電話番号が**任意**であることを確認(必須マークなし)
5. 住所欄が**ない**ことを確認
6. 必須項目を空で送信し「入力してください」が表示されることを確認
7. 全必須項目入力後に送信し、「お問い合わせを受け付けました。」が表示されることを確認
8. モバイル幅でレイアウトが崩れないことを確認
9. `npm run build` でビルドエラーがないことを確認

---

## 11a・11b 両方完了後の全体確認

- [ ] `/jobs/[slug]` → 「応募する」 → ページ内フォーム → 送信 → 完了メッセージ(ページ遷移なし)
- [ ] `/apply` → 404(削除されている)
- [ ] `/contact` → フォーム → 送信 → 完了メッセージ(同ページ内)
- [ ] Header のハンバーガーメニューの「お問い合わせ」リンクが `/contact` を指しているか(変更なければそのはず)

---

## 次タスク予告

**Phase 4 Task 12: 静的ページの雛形作成**

- `/message`(町からの便り): 仮テキストでレイアウトのみ(本文は役場連携待ち)
- `/privacy`(プライバシーポリシー): プレースホルダー本文(法的レビュー待ち)
- `/terms`(利用規約): プレースホルダー本文(同上)
- これら3ページが完成すると、13ルートすべての雛形が揃う
